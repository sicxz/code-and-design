> **Try this first.**
>
> Do these 3 steps before reading.
>
> 1. Find the `#` at the start of line 1 below.
> 2. Find the same words in the preview. Notice their size.
> 3. Imagine removing the `#`. Predict what would change.

## Source and preview

A Markdown file is a plain-text file. Put `#` and a space before words to make a heading. Put a blank line between paragraphs.

Compare the source with its preview. The heading marks stay in the source. The preview shows the headings and paragraphs.

<!-- source-preview -->

> **Aside.** The marks belong to the source. The preview is another way to read that same file.
