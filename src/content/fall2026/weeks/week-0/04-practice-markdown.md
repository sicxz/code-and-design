The companion left some notes. They are plain text so far, so every line looks the same. Add the Markdown marks and watch the preview change as you type.

## Try it

1. Put `#` and a space before the first line, `Notes on what needs fixing`. The preview shows it as the largest heading.
2. Put `##` and a space before `The page with no headings`. The preview shows a smaller heading.
3. Put `##` and a space before `The list that is not a list`. The preview shows a second smaller heading.
4. Put `-` and a space before each of the last three lines. The preview shows a bulleted list.
5. Select **Reset the notes**. The notes go back to plain text, and you can start over.

<!-- live-markdown -->

Nothing you type here is saved. Your real words go in `week-0/introduction.md`, in your repository.
